// TROPE theme: the shared venue shell, TROPE brand pack.
// Palette/typography are PROVISIONAL (extracted from the logo) until the
// TROPE branding PDF arrives.
import '@/venue/styles/venue-global.scss';
import router from '@/venue/router';
import store from '@/TheGate/store';

export { router, store };
