// COCOYA (liquid-shader hero) theme.
// Same shared venue shell and COCOYA brand pack as the `cocoya` theme; the only
// difference is the hero treatment (see content.js -> hero.motif === 'shader').
// Re-exporting from ../cocoya also runs its venue-global.scss side-effect import.
export { router, store } from "../cocoya";
