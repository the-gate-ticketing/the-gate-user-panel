// Build-time metadata for the COCOYA shader variant — identical brand to the
// `cocoya` theme (title, fonts, favicon). Loaded by vue.config.js (CommonJS).
module.exports = require("../cocoya/theme.config");
